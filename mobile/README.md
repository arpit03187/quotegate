# QuoteGate mobile — owner approver

Expo app. Queue on the phone; approve or reject a proposal; resume the LangGraph interrupt on the API.

```bash
cd mobile
npm install
EXPO_PUBLIC_API_URL=http://localhost:8000 npx expo start
```

On a physical device, point `EXPO_PUBLIC_API_URL` at your machine’s LAN IP, not localhost.
